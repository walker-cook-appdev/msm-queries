<h1>
  Actor #<%=@the_actor.at(0).id%> details
</h1>

<a href="/directors" class="btn btn-block btn-outline-secondary">
  Go back
</a>

&nbsp;|&nbsp;

<a href="/delete_director/16294" class="btn btn-block btn-outline-secondary">
  Delete director
  <small>(Building this link is not required. It's an optional stretch goal; but, you have the tools if you want to give it a try.)</small>
</a>

<hr>

<dl>
  <dt>
    Name
  </dt>
  <dd>
    <%=@the_actor.at(0).name%>
  </dd>

  <dt>
    Dob
  </dt>
  <dd>
    <%=@the_actor.at(0).dob%>
  </dd>

  <dt>
    Bio
  </dt>
  <dd>
    <%=@the_actor.at(0).bio%>
  </dd>

  <dt>
    Image
  </dt>
  <dd>
    <img src="http://robohash.org/<%=@the_actor.at(0).id%>" style="width:250px;">
  </dd>

  <dt>
    Created at
  </dt>
  <dd>
    <%=@the_actor.at(0).created_at%>
  </dd>

  <dt>
    Updated at
  </dt>
  <dd>
    <%=@the_actor.at(0).updated_at%>
  </dd>
</dl>

<hr>

<h2>Filmography</h2>

<table border="1">
  <tr>

    <th>
      Title
    </th>

    <th>
      Year
    </th>

    <th>
      Director
    </th>

     <th>
      Character Name
    </th>

  </tr>

<%@characters.all.each do |character|%>
  <tr>
    <td>
      <%=Movie.where(:id => character.movie_id).at(0).title%>
    </td>

    <td>
      <%=Movie.where(:id => character.movie_id).at(0).year%>
    </td>

    <td>
      <%=Director.where(:id => Movie.where(:id => character.movie_id).at(0).director_id).at(0).name%>
    </td>

    <td><%=character.name%></td>

    <td>
      <a href="/movies/<%=character.id%>">
        Show details
      </a>
    </td>
  </tr>
  <% end %>
</table>